<h1><%= @user.name %></h1>
<% if @user.avatar.attached? %>
  <%= image_tag @user.avatar, alt: 'User avatar' %>
<% end %>
<% if @user.motto.attached? %>
  <a href="<%= rails_blob_path(@user.motto, disposition: "attachment") %>">Download motto</a>
<% end %>
