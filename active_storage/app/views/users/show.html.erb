<h1><%= @user.name %></h1>
<% if @user.avatar.attached? %>
  <%= image_tag @user.avatar, alt: 'User avatar' %>
<% end %>
<% if @user.color.attached? %>
  <a href="<%= rails_blob_path(@user.color, disposition: "attachment") %>">Download CSS with the color</a>
<% end %>
